export interface Product {
    name: string;
    price: number;
    stock: number;
    image: string;
    brand: string;
    category: string;   
}

export interface Categories {
    category: string;
    order: string;
    price: string;
    establecimiento: string;
}